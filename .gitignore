# data (never commit tiles / orthomosaic) - raw package extracted at the repo root only
/data/
# web map: 512 px JPEGs are only an input of scripts/build_web_map.py (the site uses web/data/mosaic)
web/data/tiles/
*.tif
*.tiff
*.zip
# models
*.pt
runs/
# python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
# os
.DS_Store
# outputs (except final deliverables in repo root)
out/

# yolo dataset (generated)
dataset/
dataset_multi/
